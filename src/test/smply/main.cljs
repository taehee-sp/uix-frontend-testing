(ns test.smply.main
  (:require [test.smply.hello-test]
            [test.smply.domains.saas.saas-list.saas-list-item-test]
            [test.smply.domains.saas.saas-list.saas-list-test]))

(defn init [])